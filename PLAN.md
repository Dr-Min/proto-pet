# Proto Pet — 마스터 플랜

> 이 문서는 기획 리드(Claude)가 관리한다. 코덱스는 각 Phase의 사양을 이 문서 기준으로 구현한다.
> 확정 수치는 그대로 쓰고, `TBD`는 구현 시점에 기획 리드가 채운다.

## 0. 비전

진짜 애완동물을 키우는 것처럼 느껴지는 웹 육성 게임. 하찮게 생긴 말랑한 생명체를
먹이고 재우고 쓰다듬고 놀아주며 키우고, 다 큰 아이를 전투에 내보낸다.
**육성(관계)이 본체, 전투는 육성의 결과가 드러나는 무대다.**

## 1. 불변 원칙 (모든 Phase에 적용)

1. **행동이 게이지보다 먼저.** 상태는 스탯창이 아니라 몸짓·대사·외형으로 먼저 드러난다.
   스탯 패널 금지 (DESIGN.md와 동일).
2. **펫에게 거부권.** 배부르면 안 먹고, 안 졸리면 안 자고, 피곤하면 안 뛴다.
   도구가 아니라 생명체다.
3. **숨겨진 정보는 관찰로 발견.** 좋아하는 음식, 성격 특성은 UI에 미리 알려주지 않는다.
   발견의 순간이 애착을 만든다.
4. **케어 이력이 외형에 새겨진다.** 특성 마크(볼터치/배 점/처진 귀)처럼, 키운 방식이
   몸에 남는다. 진화는 "형태 교체"가 아니라 "누적 변화"다.
5. **아트 에셋 0장 원칙.** 모든 시각 요소는 절차적(코드) 렌더링. 새 단계/파츠/소품도
   파라미터와 도형으로 만든다.
6. **모바일 우선.** 터치 타겟 44px+, 320px 폭에서 가로 스크롤 금지, 포인터 이벤트 통합.
7. **과금 원칙 (미래).** 꾸미기·공간·슬롯·AI 구독만. 스탯/강화 판매 절대 금지.

## 2. 펫 보이스 가이드 (모든 신규 대사의 기준)

**1인칭 자기관찰 보고체** — 자기 상태를 남 일처럼 건조하게 보고한다.

- 좋은 예: `다리 있음`, `나 내려놔짐`, `발이 없어짐`, `나 공 아님`, `생각이 없다`
- 존댓말 금지, 마침표 없음, 2~6어절
- 시스템/설계 용어 금지 — 펫은 '돌봄', '루틴', '기질', '단계' 같은 단어를 모른다
- 감탄 `!!`, 여운 `…`, 능청 `;;`
- 기억 한 줄(memoryLine)도 펫 시점의 기억이다 (관찰자/사육사 시점 금지)

## 3. 현재 구현 상태 (2026-07-06 기준)

- 절차적 펫 렌더링: 블롭 몸통, 고양이 귀(움찔), 테이퍼 꼬리(살랑), 절차적 4족 보행,
  말랑 스프링, 시선 추적, 깜빡임
- 인터랙션: 탭 놀래키기, 문지르기 쓰다듬기, 잡아서 들기/던지기(바운스 물리)
- 욕구: 밥/기운/친함(유대) + 실시간 감소 + 오프라인 경과 시뮬레이션 + localStorage 저장
- 케어: 밥주기(사료 3종+최애 음식), 재우기, 돌봄 루틴 보너스, 기억 한 줄
- 성격 특성: cuddly/foodie/mellow — 케어 이력으로 해금, 행동 가중치+대사+외형 마크 반영
- 유전: 시드 기반 개체(털색/귀/눈/체형/꼬리/최애음식), `?seed=` 테스트
- 거칠게 다루기 ↔ 유대 연동 (roughness, 친하면 반감+신남)
- 놀이: 공 던져주기(fetch) — 추격/회수/거부/포기, fetchCount 영구 저장
- 배포: main 푸시 → Vercel 자동 배포 (proto-pet-two.vercel.app)

## 4. 로드맵

### Phase 1 — 성장 단계 (다음 작업, 사양 확정)

**의도**: 축적(careStats)을 소비하는 시간축. "얘가 컸어"의 순간. 에셋 추가 0
(전부 기존 리그 위의 배율 테이블).

**단계**: `egg` → `baby` → `adult` (노인/은퇴는 2세 시스템과 함께 후속 Phase).

**저장**: careStats에 `stage`, `stageChangedAt`, `hatchWarmth` 추가.
기존 세이브 마이그레이션: stage 없으면 알로 되돌리지 말고 성장 조건 재평가해서
baby 또는 adult 배정 (이미 키우던 유저 보호).

**egg (알)**
- 렌더링: 타원 1개 + 유전자 털색 틴트 + 점무늬 2~3개(유전 시드). 그림자 있음.
  가끔 혼자 흔들 (기존 wobble 재활용)
- 상호작용: 문지르기 = 품기. `hatchWarmth` 0→1 (문지르기로 +, 초당 -0.004 감쇠).
  탭하면 콩 하고 흔들리기만. 드래그 이동 가능(던지기는 페널티 없이 무시 — 알은 안 날아감)
- 버튼: 밥주기/재우기/공놀이 전부 비활성 스타일. 누르면 캡션 대신 알이 살짝 흔들
- 부화 조건: `hatchWarmth >= 1` AND 알 상태로 실시간 90초 경과
- 부화 연출: 흔들림 점점 커짐 → 금 2~3개(선) → 팡(먼지 파티클 6개) → 아기가
  찌부 스프링으로 등장, caption `…누구세요`, memoryLine `알에서 나온 날`, 하트 3개
- 알 상태의 memoryLine 기본값: `아직 세상 구경 전`

**baby (아기)**
- 비율 (기존 값에 곱): 몸 r ×0.62, 눈 크기 ×1.35, 눈 위치 아래로(fy + r×0.06),
  귀 ×0.85, 꼬리 길이 ×0.7, 다리(발 y 오프셋) ×0.6
- 행동 보정: 이동속도 ×0.85, 자빠짐 확률 ×1.8, zoomies 가중치 ×1.3
- 케어 보정: 기운 감소 ×1.25 (금방 지침), 배고픔 감소 ×1.2
- 공놀이 버튼 잠금: 비활성 스타일, 누르면 caption `아직 공 몰라`
- 성장 조건 (모두 충족 시 다음 수면에서 성장):
  부화 후 실시간 24시간 경과 AND 아래 중 2개 이상
  — mealsFed ≥ 6 / napsTaken ≥ 3 / petStrokes ≥ 150
- 성장 연출: sleep 중 반짝(하트+먼지) → 깨어나며 비율이 1.5초에 걸쳐 트윈 →
  caption `나 좀 큰 듯`, memoryLine `어른이 된 날`

**adult (성체)**
- 비율: 몸 r ×1.12, 다리 ×1.15, 꼬리 ×1.1
- 행동 보정: 이동속도 ×1.1, 자빠짐 확률 ×0.7
- 공놀이 해금. (후속 Phase에서 전투 참가 자격 = adult)

**개발 편의**: `?stage=egg|baby|adult` URL 파라미터로 강제 프리뷰(저장 안 함),
`?seed=`와 조합 가능. 성장 대기시간은 `?fast=1`이면 90초/24h → 10초/60초로 단축.

**구현 노트**: 비율은 상수 테이블 `STAGE_SCALES`로 한곳에 모을 것. 렌더링 곳곳에
배율을 심지 말고 pet.r/눈/귀/꼬리/발이 읽는 소스 값에 단계 배율을 곱해서 공급.
트윈은 현재 배율 → 목표 배율 lerp.

### Phase 2 — 스탯 씨앗 가시화 (소형)

- fetchCount가 몸짓에 반영: 공놀이 반복할수록 추격 속도 +, 공 앞 자빠짐 확률 -,
  회수 성공 시 가끔 caption `이제 좀 익숙함`
- 원칙 1 준수: 숫자 노출 금지, 몸짓 변화로만
- 이것이 순발력 스탯의 원형. 전투 Phase에서 수치화

### Phase 3 — AI 대사 (백엔드 필요, 착수 전 기획 리드와 아키텍처 확정)

- 캡션/기억 한 줄의 일부를 Claude Haiku 생성으로: 입력 = 욕구 상태 + 특성 +
  최근 기억 + 보이스 가이드, 출력 = 보이스 가이드 준수 한 줄
- 프록시 서버 필수 (API 키 클라이언트 노출 금지), Vercel serverless function 예정
- 프롬프트 캐싱(고정 지시문 앞부분), 유저별 호출 제한
- 실패/오프라인 시 기존 정적 대사로 폴백 — AI는 향신료, 정적 대사가 뼈대

### Phase 4 — 전투 (관전 + 제한 개입)

- 자동전투: 펫이 성격·특성대로 알아서 싸움 (겁쟁이는 도망다니다 카운터,
  foodie는 가끔 딴짓). 절차적 애니메이션이 무대
- 개입: 쿨다운 있는 지시 2~3개 + 응원. **유대 낮으면 지시를 씹는다** (핵심 보상)
- 컨디션 연동: 욕구 상태가 전투력에 직결. 다치면 회복 기간
- 스타일 상성(파워/스피드/테크닉)은 보류 결정 유지 — 스탯만으로 시작
- 참가 자격: adult

### Phase 5+ — 백로그 (착수 시 기획 리드가 사양화)

- 획득 경로: 입양 세리머니(첫 펫이 다가옴), 산책터 알 발견, 2세/세대 계승
- 산책 장소 시스템 (장소별 놀이/유전자 풀)
- 꾸미기: 모자/목도리(버렛 체인 재활용) — 앵커 포인트 선작업 필요
- 수익화: 코스메틱/공간/슬롯/AI 구독 (원칙 7 준수)
- 파일 구조: src/ 모듈이 더 커지면 ES 모듈 전환 검토

## 5. 코덱스 작업 규칙

1. **커밋 금지.** 워킹 트리에 남기면 기획 리드가 검수 후 커밋한다
2. 이 문서와 DESIGN.md의 원칙·토큰을 따른다. 새 색은 DESIGN.md 표에 먼저 추가
3. 신규 대사는 §2 보이스 가이드 준수
4. 판정/보상 로직은 care.js, 물리/행동은 sim.js, 그리기는 render.js, 입력은 input.js
   — 기존 역할 분리 유지
5. 검증: `node --check` 전 파일 + 정적 하네스(scripts/) + 가능하면 브라우저 QA.
   완료 보고에 파일별 변경 요약
6. 저장 스키마 변경 시 기존 세이브 마이그레이션 필수 (Number 가드 패턴 준수)
